# Davinci 공식 웹사이트 Homepage Copy Deck

> 작성일: 2026-06-02
> 용도: 공식 홈페이지 제작 시 그대로 옮길 수 있는 section별 완성 copy
> Source of truth: `DAVINCI-OFFICIAL-WEBSITE-FINAL-PLAN-2026-06-02.md`
> 제외 범위: 실제 사이트 구현, visual mockup, 배포

---

## 1. Hero Copy

Eyebrow:

```text
AI Runtime Orchestration Platform
```

Headline:

```text
여러 AI의 장점을
하나의 생산 라인으로.
```

Body:

```text
Davinci는 Claude Code, Codex CLI, Gemini CLI, MCP/local tools를 하나의 runtime-aware workflow로 연결합니다. 기획, 구현, 검증, 배포까지 이어지는 AI-native software production pipeline입니다.
```

Proof line:

```text
60+ workflow skills · 33+ expert agents · 20+ hook guardrails · 3-Tier verification · Native Studio
```

Primary CTA:

```text
See the Pipeline
```

Secondary CTA:

```text
Download Davinci
```

---

## 2. Trust Gap Copy

Headline:

```text
AI는 빨라졌지만, production은 여전히 검증을 요구합니다.
```

Body:

```text
AI coding agent는 코드를 더 빠르게 만들지만, output accuracy, context retention, rule adherence, deployment stability는 여전히 검증을 요구합니다. Davinci는 이 trust gap을 개인의 주의력 문제가 아니라 workflow와 guardrail의 문제로 다룹니다.
```

Microcopy:

```text
속도는 시작입니다. 신뢰는 routing, evidence, verification이 이어질 때 만들어집니다.
```

---

## 3. Unified AI Runtime Pipeline Copy

Headline:

```text
Davinci는 AI들을 하나의 pipeline으로 운영합니다.
```

Body:

```text
Claude, Codex, Gemini, MCP/local tools는 서로 다른 강점을 갖습니다. Davinci는 이 runtime들을 경쟁시키지 않고, intent와 역할에 맞게 연결해 하나의 production line 안에서 이어지게 만듭니다.
```

Runtime lane copy:

| Lane | Copy |
|------|------|
| Claude lane | Deep reasoning, planning, complex implementation |
| Codex lane | Local code edit, run, automation |
| Gemini lane | Broad context, research, extension workflow |
| MCP/local lane | Docs, browser, symbol search, deployment, verification tools |

Pipeline label:

```text
Human Intent -> Runtime-aware Routing -> Evidence Handoff -> Verification Gate -> Production Output
```

---

## 4. Agent Harness Copy

Headline:

```text
좋은 모델만으로는 좋은 software pipeline이 되지 않습니다.
```

Body:

```text
AI가 production-ready software를 만들려면 모델을 감싸는 실행 구조가 필요합니다. Davinci는 orchestration, tools, memory, context, prompt construction, output parsing, state, error handling, guardrails, verification, subagent orchestration을 하나의 Agent Harness로 묶습니다.
```

Proof copy:

```text
PM Agent, Reflexion, Handoff Chain, hooks, skills, agent persona가 같은 pipeline 안에서 작동합니다.
```

---

## 5. Runtime-Aware Routing Copy

Headline:

```text
요청은 workflow로, workflow는 agent와 runtime으로 라우팅됩니다.
```

Body:

```text
Davinci는 자연어 intent를 `/da:*` workflow로 연결하고, 작업 복잡도와 필요한 evidence 수준에 따라 skill, agent, runtime, MCP/local tool, verification tier를 선택합니다.
```

Step copy:

```text
Intent -> Skill -> Agent -> Runtime capability -> Tool context -> Verification tier
```

Microcopy:

```text
사용자는 runtime을 들고 다니지 않습니다. Davinci가 작업의 성격에 맞는 route를 구성합니다.
```

---

## 6. Verification-First Copy

Headline:

```text
속도 이후의 문제는 검증입니다.
```

Body:

```text
Davinci는 생성된 결과를 바로 끝으로 보지 않습니다. PM Agent, 3-Tier verification, hook guardrails, Reflexion loop, Handoff Chain으로 다음 단계에 넘길 수 있는 evidence를 만듭니다.
```

Before / After:

| Before | After |
|--------|-------|
| isolated AI output | evidence-backed handoff |
| manual checking | structured Verification Gate |
| repeated context loss | continuous workflow state |

---

## 7. Native Studio Copy

Headline:

```text
CLI의 힘을 native studio에서 보이게 만듭니다.
```

Body:

```text
Davinci는 terminal agent의 힘을 숨기지 않습니다. session, agent tree, timeline, cost, tool output, design/code panel을 하나의 native interface에서 추적 가능하게 만듭니다.
```

Feature copy:

```text
AI runtime의 실행 상태를 보고, evidence를 확인하고, 다음 action을 이어갈 수 있는 production studio입니다.
```

---

## 8. Design-To-Code Copy

Headline:

```text
Design intent까지 pipeline 안으로 들어옵니다.
```

Body:

```text
Davinci는 code generation에만 머물지 않습니다. idea, product requirement, UX direction, visual intent, code workflow가 같은 production pipeline 안에서 이어지도록 설계됩니다.
```

Microcopy:

```text
Create Studio와 Design-to-Code는 부가 기능이 아니라, software production line의 앞단입니다.
```

---

## 9. Comparison Copy

Headline:

```text
비교는 도구 수가 아니라 운영 구조에서 갈립니다.
```

Body:

```text
Davinci는 Cursor, Windsurf, Claude Code, Codex, Gemini를 깎아내리지 않습니다. 각 tool/runtime의 강점을 인정하되, Davinci의 차별점을 multi-routing, evidence handoff, verification-first production, native studio로 설명합니다.
```

Axis copy:

```text
Single assistant vs multi-runtime orchestration
Code generation vs production pipeline
Manual supervision vs structured verification
Editor surface vs native production studio
Isolated output vs Evidence Handoff
```

---

## 10. Proof Copy

Headline:

```text
이미 제품 안에 들어간 운영 구조입니다.
```

Proof blocks:

| Metric | Copy |
|--------|------|
| 60+ workflow skills | intent를 production workflow로 바꾸는 skill system |
| 33+ expert agents | 역할별 agent persona와 handoff 구조 |
| 20+ hook guardrails | 실행 전후 안전장치와 workflow boundary |
| 12K+ core harness LOC | orchestration, state, routing, verification 기반 |
| 3-Tier verification | 결과물을 다음 단계로 넘기기 전 검증 gate |

Guardrail:

```text
숫자는 자랑이 아니라, Davinci가 실제 운영 구조를 갖췄다는 proof로만 사용합니다.
```

---

## 11. Workflow Operations Copy

Headline:

```text
작업은 코드가 아니라 팀의 workflow로 이어집니다.
```

Body:

```text
Davinci는 AI output을 isolated result로 남기지 않습니다. Git Worktree, branch history, PR Review, Jira issue, Slack notification, deploy/monitor routine까지 이어지는 team operations layer 안에서 작업을 관리합니다.
```

Feature copy:

| Flow | Copy |
|------|------|
| Git Worktree | session과 작업 단위를 독립된 worktree/branch 흐름으로 분리합니다. |
| Git history | AI가 만든 변경을 추적 가능한 history와 handoff context 안에 남깁니다. |
| PR Review | PR description, test plan, reviewer handoff를 review-ready package로 정리합니다. |
| Jira | issue, status, workflow event를 AI 작업 흐름과 연결합니다. |
| Slack | workflow progress, error notification, team visibility를 Slack에서 이어갑니다. |
| Deploy / Monitor | verify 이후 배포와 운영 확인까지 production line에서 이어집니다. |

Microcopy:

```text
AI가 코드를 만드는 것보다 중요한 것은, 그 작업이 팀의 운영 시스템 안에 안전하게 남는 방식입니다.
```

CTA:

```text
See Workflow Operations
```

---

## 12. Final CTA Copy

Headline:

```text
코드 생성은 시작일 뿐입니다.
```

Body:

```text
Davinci로 여러 AI runtime의 장점을 하나의 검증 가능한 production pipeline 안에서 운영하세요.
```

CTA set:

```text
Install Framework
Download App
Read Docs
Request Demo
```

Closing:

```text
Human intent. AI execution. System-level verification.
```

---

## 13. Copy QA

이 copy deck은 아래 기준을 충족해야 한다.

- `AI Runtime Orchestration Platform` category가 Hero에 들어간다.
- `여러 AI의 장점을 하나의 생산 라인으로.`가 최종 H1으로 고정된다.
- Claude/Codex/Gemini/MCP는 runtime lane으로 표현된다.
- `Runtime-aware Routing`, `Evidence Handoff`, `Verification Gate`가 반복된다.
- `Git Worktree`, `PR Review`, `Jira`, `Slack`은 platform completeness proof로 표현된다.
- 특정 runtime을 낮추지 않는다.
- 사용자를 낮추지 않는다.
- 완전 자동 개발, 모든 tool 대체 같은 과장 claim을 쓰지 않는다.

최종 판정:

```text
이 homepage copy deck은 Davinci 공식 웹사이트의 최종 메시지를 실제 section copy로 옮길 수 있는 기준이다.
```
